<UserControl x:Class="ewoxej_gitsame.SourceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:ewoxej_gitsame"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="400" >
    <Border BorderBrush= "{StaticResource PrimaryHueDarkBrush}" BorderThickness="1">
        <Grid Margin="10 10 10 10" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Name="lTitle" Grid.Column="0" HorizontalAlignment="Left" 
                               Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                               VerticalAlignment="Center">Источник Х</TextBlock>
                    <Button Margin="10 5 5 5" Grid.Column="3" Click="DeleteItem_Clicked">
                        <materialDesign:PackIcon Kind="ClearCircle"/>
                    </Button>
                    <Button Margin="5 5 5 5" Grid.Column="2" Click="AddItem_Clicked">
                        <materialDesign:PackIcon Kind="Add"/>
                    </Button>
                    <Button Name="bDatabase" Margin="5 5 10 5" Grid.Column="1" Click="UseDb_Clicked">
                        <materialDesign:PackIcon Kind="Database"/>
                    </Button>
                </Grid>
                <ListBox Name="lbSource" Grid.Row="1" ItemsSource="{Binding Path=SourcesList}"
                         SelectedItem="{Binding Path=CurrentItem}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <materialDesign:PackIcon Kind="{Binding Path=IconKind}"/>
                                <TextBlock Text="{Binding Path=FriendlyName}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <CheckBox Margin="10 10 10 10" Grid.Row="2" Content="Добавить в локальную БД" IsChecked="{Binding Path=AddToDb, Mode=TwoWay}"></CheckBox>
            </Grid>
        </Grid>
    </Border>
</UserControl>
